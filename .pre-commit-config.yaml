repos:
  - repo: local
    hooks:
      - id: no-gstack-edits
        name: Protect gstack/ (read-only upstream reference)
        language: python
        entry: python scripts/check-gstack-readonly.py
        pass_filenames: false
        always_run: true
