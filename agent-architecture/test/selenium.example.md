# Selenium Example

Use Selenium only when the project already standardizes on Selenium or the
active profile selects it.

```bash
mvn test -Dtest=*Selenium*
```

Expected evidence:

- Test command.
- Driver/browser version source.
- Target environment.
- Pass/fail result.
- Artifact paths when produced.
