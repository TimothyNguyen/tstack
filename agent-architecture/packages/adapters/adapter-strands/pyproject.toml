[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "adapter-strands"
version = "0.1.1"
description = "Agent-architecture adapter for Strands integration"
requires-python = ">=3.8"
license = "Apache-2.0"

[tool.setuptools]
packages = []
