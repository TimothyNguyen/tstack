# Workflows

Workflow definitions belong here when they compose multiple skills, agents, stacks, domains, adapters, or tool providers into an ordered procedure.

Keep workflow files declarative. Runtime execution belongs in `agent-harness`; registry export belongs in `agent-registry`.
